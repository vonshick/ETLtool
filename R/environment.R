# Below you can find the list of environment variables that
# need to be set before processing starts

# Sys.setenv("JSON_DIRECTORY" = "/json/data/path")
# Sys.setenv("DB_HOST" = "host")
# Sys.setenv("DB_USER" = "user")
# Sys.setenv("DB_PASSWORD" = "password")
# Sys.setenv("DB_NAME" = "name")
# Sys.setenv("DB_PORT" = "port")
# Sys.setenv("DB_SCHEMA" = "schema")
